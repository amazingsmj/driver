[Unit]
Description=Windshaft Map Tiler
After=network.target

[Service]
User={{ ansible_ssh_user }}
Group={{ ansible_ssh_user }}
WorkingDirectory=/opt/windshaft
ExecStart=/usr/bin/node /opt/windshaft/app.js
Restart=always
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
